# 个人 A 股量化系统审计与公开数据优先整改方案

> 状态：方案审计完成；整改、运行验证与采购均未执行。
>
> 审计日期：2026-09-23（北京时间）
>
> 审计基线：`anwu-d/docs@afbebb3dd096d4f3b3da3d99f5a29e17fbc90d8d`
>
> 适用目标：个人自用、公开数据优先、复用成熟开源软件；AI 已准备，GPT 负责计划，DeepSeek 或 MiMo 负责执行。
>
> 修订：v2，依据用户澄清调整成本含义；取消上一版自行设定的 AI 月费和每日处理量上限。
>
> 本文是原设计的审计补充及整改建议，不代表原设计已经修改，也不代表系统通过工程验收。

## 1. 审计结论

**建议先整改设计，再实施精简 MVP。**

保留原方案的数据质量、历史可见性、可复现研究和个人复盘方向。首期收敛到“日线数据 → 持仓与收益计算 → 简单策略回测 → 相关公告 → 日度报告”，采用 GPT 计划与审核、DeepSeek/MiMo 执行的协作方式；是否扩大全市场研究由数据质量和任务需要决定，不以压缩 AI 开支为前提。独立常驻服务和分钟线按需求引入。

主要阻断项不是工具数量不足，而是历史 PIT 证据不足、标签与执行时点不一致、不可成交样本处理不清，以及自动修复可能反复消费样本外结果。增加模型或第二套回测引擎不能自动解决这些问题。

成本优化重点是复用公开历史数据及每日发布数据包，减少付费数据订阅与重复采集。用户已准备 AI 模型，本方案不再规定 AI 每月 30–50 元或总计 0–70 元的预算。模型调用保留用量记录、可配置重试和并发控制，具体额度按已有服务配置。

## 2. 范围、证据与限制

### 2.1 审计对象

- [主设计](../a-share-quant-system-design.md)：重点检查 §5–8、§11–16。
- [数据层调研](../research/01-data-layer.md)：数据覆盖、PIT、存储。
- [回测与因子调研](../research/02-backtest-factor.md)：执行口径、验证与工具选型。
- [新闻事件调研](../research/03-news-events.md)：采集、去重、事件证据。
- [Agent 调研](../research/04-agent-orchestration.md)：运行时、模型、预算。
- [原验收记录](../t6-t10-acceptance.md)：验收口径。

基线仓库只有上述六份 Markdown 文档，没有可运行应用、部署配置或测试结果。本次为重点设计审计，未逐一验证所有候选项目、许可证、数据接口、模型价格、stars 和性能数字。

### 2.2 证据分类

| 分类 | 含义 |
|---|---|
| 文档事实 | 可在固定基线的章节或配置中直接定位 |
| 外部核对 | 已对照官方文档、上游源代码或实际模型发布页 |
| 工程推断 | 由设计组合推导出的风险，须用实现验证 |
| 待实测 | 需要真实设备、数据权限或代码才能确认 |

本文 P0/P1/P2 表示**整改优先级**，与原文实施阶段编号不同：P0 阻断可信回测或实施；P1 在无人值守前完成；P2 按实际需求处理。所有发现初始状态均为 OPEN。

## 3. 发现与整改要求

### A01 · P0：历史 PIT 表不能补出不存在的历史版本

- **位置**：主设计 §5.1.2、§13.3；数据调研的 PIT 说明。
- **证据**：方案承认部分免费源仅提供最新修订值，却以披露日过滤及新建版本表作为历史防前视措施。
- **影响**：现在抓到的修订值可能被放回早期披露日期，回测使用当时未知的信息。新增 `fetched_at` 或 `_next` 本身无法修复。
- **整改**：
  1. 保存来源文档编号、内容哈希、原始披露时间、修订披露时间、首次采集时间、版本和时区。
  2. 分别定义“市场已公开时间”和“本系统实际获得时间”。历史研究与实时重放使用不同可见性口径，并记录所用口径。
  3. 只有日期、没有披露时刻时，采用明确的保守下一交易时段规则，不允许默认当日开盘可用。
  4. 无法恢复原始版本的字段标记为 `historical_pit_unverified`，阻止进入声称无前视的历史研究；MVP 可暂不使用财务因子。
- **验收**：构造先披露、后更正的两版本案例；更正前查询不出现更正值。同日多版本、收盘后公告和时间未知均有确定结果。

### A02 · P0：预测标签与成交口径没有对齐

- **位置**：主设计 §5.3.1，开盘买入、收盘卖出配置与收盘收益标签并列。
- **外部核对**：Qlib 的 `Ref($close,-2)/Ref($close,-1)-1` 表示 T+1 收盘至 T+2 收盘收益。[S1]
- **影响**：研究指标和策略收益衡量不同交易区间，不能标为“与执行假设一致”。
- **整改**：定义统一执行契约：信号截至时间、最早下单时间、买卖价格、持有期、交易日历、可卖数量、费用及部分成交规则。标签可以作为预测代理，但必须说明与最终执行收益的差异。
- **验收**：用小型手工行情逐笔计算信号、持仓、费用和净值，与引擎输出对齐；覆盖收盘信号、跨节假日及不可当日卖出的持仓。

### A03 · P0：剔除涨跌停样本可能美化回测

- **位置**：主设计 §13.3 第 4 条、§12.3 股票池配置。
- **影响**：事后删除未成交样本可能产生选择偏差；跌停卖不出的旧持仓若被移除，净值失真。
- **整改**：分开保存信号股票池、下单候选、成交结果和持仓。只用决策时已知条件生成股票池；成交时检查方向、可成交量、停牌及对应历史制度。旧仓未成交则继续持有和估值，记录未成交原因。
- **验收**：买入失败、卖出失败、部分成交、停牌及恢复交易案例均守恒；现金、数量、费用和净值可对账。日线无法判定的盘中成交应采用公开说明的保守假设。

### A04 · P0：自动修复与封存样本外互相冲突

- **位置**：主设计 §8.8、§12.3、§13.4–13.5。
- **影响**：Agent 根据样本外表现反复改策略，等价于把测试集变成训练反馈。
- **整改**：
  - 分离训练、开发验证和最终封存测试；冻结代码、参数与数据版本后才开最终测试。
  - 实现错误可在开发集修复；策略经济表现不佳记录为失败，不应循环“修到通过”。
  - 最终测试访问和结果回流有日志；已消费的测试窗口不再称为未见样本。
  - 按标签跨度处理时间切分边界，防止跨界标签泄漏。
  - 保存失败、否决及参数变体。删除“入库因子数量”硬指标。
- **验收**：开发 Agent 不能读取最终测试结果；每次实验能定位数据分段和试验家族；零有效因子允许正常验收。

### A05 · P1：实施阶段与数据复用策略需统一

- **位置**：主设计 §8、§14、§15、附录 B。
- **证据**：MVP 已包含多个采集库、研究框架、追踪、本地模型；附录 B 又把编排放入 P0，而 §14.2 明确 MVP 不做 Agent 自动化。
- **整改**：统一阶段清单，采用第 4 节组件表；优先接入公开 Qlib 数据包而非从零重建全量采集。GPT/执行模型协作从 G0 开始，复杂运行时是否引入按实际需要决定。
- **验收**：首期一个明确的历史数据底座、一个主回测引擎、一个定时入口；模型分工和输入输出可追溯；AI 不可用时核心数字报告仍能生成。

### A06 · P1：内存及成本估算缺少完整口径

- **位置**：主设计 §6.1、§6.4。
- **证据**：64GB 档分配合计 68GB；14B Q4 写成 4–6GB。所查量化发布的 Q5_K_M 文件约 10.51GB，运行内存还包括 KV cache 等。[S2]
- **整改**：依据具体模型文件和实际峰值测算，不把模型权重当作总内存。先测已有设备，训练、回测与模型推理错峰。取消首期购买 32/64GB 设备的前置条件。
- **验收**：记录设备、模型版本、上下文长度、耗时、内存峰值和交换内存；连续运行不因资源不足中断。预算记录区分输入、输出、缓存、重试、电费和已有订阅。

### A07 · P0（复制部署前）：速查命令尚不是运行手册

- **位置**：主设计 §7.1、§15.3。
- **证据**：存在 `minio/minio:RELEASE.2026-xx` 占位版本及未交付的 `data.etl` 等模块；初始化流程未提供完整项目文件。Qlib 上游工作流入口是 `qrun`，原文命令须按锁定版本验证。[S3]
- **整改**：拆为“架构示例”“待实现接口”“已验证操作”。已验证部分必须包含依赖锁定、所需文件、版本、预期输出、失败处理及恢复步骤；不要同时以容器和裸机重复启动同一服务。
- **验收**：从空目录在目标环境执行通过并留下日志；没有占位版本、缺失模块或未说明的前置条件。当前不能标记此项已通过。

### A08 · P1：许可证策略不适合个人自用

- **位置**：主设计 §16.3。
- **证据**：GPL/AGPL 被整体列为红区，AGPL 描述为“网络服务也算分发”。
- **整改**：
  - GPL 个人本地使用、修改不因使用本身要求公开所有个人代码；Backtrader 可重新列入候选。[S4][S6]
  - AGPL 应按条款区分修改、网络交互和源代码提供义务，不能简单等同于分发。[S5]
  - 分开记录开源代码许可、模型许可、数据使用条款、服务订阅条款。
  - 无许可证不等于允许复制；附加非商业限制须检查具体用途，进程隔离不自动消除许可义务。
  - 个人使用不自动获得上游数据抓取或再分发授权。
- **验收**：实际采用的每个依赖有版本、许可证原文链接和使用方式；将本地自用、代码公开发布、对外网络服务分别判断。本审计不作全依赖法律合规认证。

### A09 · P1：存储并发与备份边界不足

- **位置**：主设计 §5.1、§7.1、§13.7。
- **影响**：多个 Agent 直接写同一 DuckDB 文件可能与嵌入式并发模式不符；同机快照不能抵御整机或磁盘损坏。
- **整改**：单一写入者产生不可变 Parquet 快照及 manifest，读任务绑定 snapshot_id；SQLite 保存任务元数据。仅在证明需要多进程写服务后考虑数据库升级。[S7]
- **验收**：读任务不会读到半发布数据；失败重跑不重复入库；从独立备份恢复原始数据、元数据和报告。备份从首期开始，建议目标 RPO 24 小时、RTO 1 天，以实测确认。

### A10 · P1：固定出报时点不等于数据已经齐备

- **位置**：主设计 §11.1。
- **影响**：不同来源更新时间不同，16:15 不能天然保证日线、两融、龙虎榜和公告齐全。
- **整改**：每个数据集记录截至时间、预期日期和就绪状态；先生成明确标注缺项的初版，随后补齐，次日晨间修订；旧数据不得伪装为当天数据。
- **验收**：模拟单源延迟或故障，报告展示缺项及版本，恢复后补跑不重复统计。

### A11 · P1：去重比例和 JSON 完整率不能代表信息正确率

- **位置**：主设计 §14.3；新闻调研 §4。
- **影响**：追求固定滤重比例可能丢掉金额、对象或披露状态变化的公告；五要素齐全仍可能抽取错误。
- **整改**：首期按来源文档 ID 和内容哈希去重；保留修订链，不凭标题相似直接丢弃。金额、单位、主体、日期须能定位原文。缺证据时输出未知。
- **验收**：固定一批含转载、更正和同标题不同内容的人工标注样本，分别统计漏检、误合并和关键字段准确性；阈值由用途确定，不能以去重率替代。

### A12 · P1：Agent 写代码与预算控制需落实到执行边界

- **位置**：主设计 §8.6、§8.8、§13.7。
- **影响**：git worktree 只隔离目录，不能限制进程访问密钥、测试集或原始数据；“coding 会话不受限”和仅预算告警可能导致资源、费用失控。
- **整改**：DeepSeek/MiMo 可执行 GPT 的已冻结计划并修复实现错误；限制工具、可写目录、网络、并发和运行时长；原始数据与封存集只读或不可见；新闻、论文中的指令不能触发工具执行。记录调用与重试；若已有服务配置硬额度，则调用前预留费用并在完成后结算。具体限额不由本文擅自设定。
- **验收**：受限任务不能修改原始数据、读取未授权密钥或封存结果；在配置额度或服务限流触发时停止新增调用，保留检查点，确定性报告继续执行。

## 4. 个人版目标架构与开源选型

| 能力 | 首期方案 | 推迟项与升级条件 |
|---|---|---|
| 数据采集 | 优先评估 investment_data Release 作为 Qlib 历史日线底座；BaoStock/AKShare 补字段与抽样对账 | 按第 11 节验收；只有明确无法补齐的缺口才考虑付费源 |
| 事实数据 | 原始响应归档、Parquet、DuckDB 只读查询 | 不引入服务数据库，除非单写入者无法满足实测需求 |
| 任务状态 | SQLite、幂等键、失败日志、一次重跑机制 | 不建设通用任务平台 |
| 回测 | 沿用 Qlib，先简单基线与手算案例 | Backtrader 可作为替代候选；不要同时维护两套。通过 A 股适配测试后再决定 |
| 因子分析 | 少量预先声明的简单因子及对照 | RD-Agent、遗传规划、自动因子生产线延后 |
| 实验追踪 | 配置、git SHA、数据快照、结果文件；需要时接本地 MLflow | 不要求常驻服务器或 LLM tracing |
| 新闻公告 | 持仓、自选、重大公告；来源 ID 与内容哈希去重 | 全市场轮询、语义去重、向量库及 RAG 按实际检索需求引入 |
| AI | GPT 负责计划、研究规范和审核；DeepSeek 或 MiMo 执行编码、数据接入、测试和报告任务 | 复用已准备的模型；不重新选型，不强加超低预算或每日数量上限 |
| 调度与展示 | Python 确定性流程、launchd、一个报告入口 | MCP 和复杂编排只在已有多个客户端复用工具时考虑 |
| 备份 | 独立介质或异地加密备份及恢复验证 | 不以同机 MinIO 替代灾难恢复 |
| 交易 | 手工持仓/成交导入，人工判断 | 首期不接自动下单 |

上述方案利用原设计已有工具，避免全面换框架增加迁移工作。对用于策略回测的缩小股票池，必须记录选择方式：当前自选股回溯只能作为探索，不能当作无幸存者偏差的全市场证据。

## 5. 公开数据优先与模型使用方案

### 5.1 数据采购原则

1. 优先复用公开 Release、开源采集器及官方公开披露信息。
2. 首先验证 investment_data 历史日线包；合格后直接供 Qlib 使用，保留原始包，不要求先全量转换为 Parquet 再转回 Qlib。
3. 对缺失的未复权价格、交易状态、行业成分历史、财务披露及事件逐项建缺口清单，再决定补充来源。
4. Tushare 等付费来源只在公开渠道无法满足已确认需求时评估；不预定某周购买，也不承诺某积分档覆盖所有接口。
5. 免费数据也统计下载、磁盘、备份和维护成本。AI 为已有能力，费用单列，不将数据省钱目标误解为降低模型能力。

### 5.2 GPT 计划、DeepSeek/MiMo 执行

| 环节 | 责任 | 交付与边界 |
|---|---|---|
| 计划与研究规范 | GPT | 明确目标、数据快照、允许数据分段、任务依赖、验收命令和成功条件 |
| 编码与运行 | DeepSeek 或 MiMo | 执行计划，提交代码 diff、日志、测试、指标及失败原因；每个任务指定一个主执行者 |
| 数值与回测 | 确定性程序 | 计算收益、费用、指标；模型不得编造或心算替代 |
| 审核与修订 | GPT | 对照固定验收标准检查证据；重大策略变更形成新实验版本 |
| 最终样本外 | 隔离验证任务 | 冻结方案后评估；结果不得回流当前策略的自动修复循环 |

任务契约至少包含 task_id、plan_version、data_snapshot_id、code_commit、allowed_paths、acceptance_commands、timeout 和 retry_policy。可在一个运行时中实现两个模型角色，无需为每个角色独立部署服务。

模型切换须携带已完成步骤、失败证据与剩余工作，不从头重复试验；GPT、DeepSeek、MiMo 均记录实际模型标识。具体调用端点、上下文和账户额度沿用用户准备的配置，本文没有验证这些账户的运行权限。

### 5.3 运行控制

- 保留输入/输出 token、重试、并发和任务耗时记录；额度按已有服务配置，不预设每日一次或每次十篇。
- 缓存键包含内容哈希、数据快照、提示版本和模型配置。
- 修复代码错误与修改研究假设分开登记；不能以提高最终测试收益为修复目标。
- 服务不可用时保留检查点，数字报告仍可生成；涉及新付费数据或硬件采购时先说明缺口和费用。

## 6. 分阶段整改与放行条件

采用阶段门，时间仅为计划参考，不按日期自动升级。

| 阶段 | 工作 | 放行证据 |
|---|---|---|
| G0：设计修订 | GPT 制定计划，DeepSeek/MiMo 执行文档与接入任务；完成 A01–A04、A07 的设计决策；统一执行与数据契约；删除冲突阶段定义 | 修订后的主设计、决策记录、待实现与已验证清单 |
| G1：复盘 MVP | 持仓导入、日线更新、质量检查、确定性报告、独立备份 | 连续 10 个交易日有报告；延迟数据明确标记；至少一次恢复成功 |
| G2：可信回测 | 简单基线、持仓现金账本、手算案例、不可成交案例、数据版本与费用模型 | 固定输入可复现；手算结果在预定义误差内；无已知前视路径 |
| G3：研究与事件闭环 | 扩展 GPT 计划与执行模型协作到公告、因子和复盘 | 固定样本评估、计划可追溯、额度配置和失败恢复通过 |
| G4：按需扩展 | 因子研究、更多数据、可选编排 | 证明新增组件解决具体瓶颈；给出新增费用、维护工作及替换方法 |

进入 G2 前不得以回测收益作为购买硬件或扩大自动化的依据。进入 G4 前不设有效因子数量、论文数量或 Agent 数量指标。

## 7. 验收矩阵

| 测试 | 关联发现 | 通过条件 |
|---|---|---|
| 修订财报时间旅行查询 | A01 | 更正前不可见更正值；无历史证据字段被阻断 |
| 信号—成交—收益对齐 | A02 | 交易时点、标签含义、持仓和费用全部可解释 |
| 停牌与涨跌停处理 | A03 | 未成交不会消失；现金与数量守恒 |
| 封存集访问记录 | A04 | 开发闭环没有消费最终测试反馈 |
| 空环境安装 | A07 | 锁定版本、必要文件、日志齐全且没有占位命令 |
| 资源测量 | A06 | 实际设备上完成目标批次，记录峰值与耗时 |
| 并发及中断重放 | A09 | 无半成品快照、无重复事实、读任务版本固定 |
| 独立备份恢复 | A09 | 核心数据、元数据、配置与报告可恢复 |
| 延迟源与断网 | A10 | 报告标记缺失与过期数据；补跑结果正确 |
| 公告更正与字段抽取 | A11 | 修订不误删，关键数字能定位证据 |
| 预算边界与在途费用 | A12 | 无预算时不新增付费任务；并发费用纳入预留 |
| Agent 权限边界 | A12 | 不能改原始数据、泄露密钥或读封存测试结果 |

运行验收由实际执行者提供日志、版本、样例和结果，用户据此决定是否进入下一阶段。本文所有运行验收均为**未执行**，不预填通过。

## 8. 后续文档修改清单

| 文件 | 修改内容 |
|---|---|
| 主设计 §5、§13 | 增加可见性时间契约、成交与股票池分离、最终测试访问规则 |
| 主设计 §6 | 更正内存规划；以实际使用量与预算上限替代笼统月费 |
| 主设计 §7、§15.3 | 精简依赖；区分示意与可执行命令 |
| 主设计 §8 | GPT 计划、DeepSeek/MiMo 执行；明确任务契约、并发、权限及修复边界 |
| 主设计 §11 | 数据就绪驱动出报、初版/修订版与缺项说明 |
| 主设计 §12–14、附录 B | 统一阶段优先级；取消因子数量验收；先通过数据与执行正确性 |
| 主设计 §16.3 | 改为按许可和使用方式判断，允许适合的个人 GPL 工具 |
| 四份 research 文档 | 同步受影响结论；未重核事实保留“待核实”，不以 stars 代替可用性 |
| t6-t10-acceptance.md | 明确原记录属于文档覆盖验收；工程验收另附实际证据 |

本文件拟提交为新增审计方案，以上修改作为后续整改任务，不声称已完成。

## 9. 来源与复核说明

外部依据在本次审计会话中查询。页面内容可能变化；实施时需为实际采用版本保存 commit、许可证或模型文件哈希。以下来源只支撑对应事实，不代表整套方案获得上游认可。

- **S1** Qlib 数据与标签定义：[官方数据文档源文件](https://github.com/microsoft/qlib/blob/main/docs/component/data.rst)、[Alpha158 实现](https://github.com/microsoft/qlib/blob/main/qlib/contrib/data/handler.py)。
- **S2** Hermes-4-14B GGUF 实际量化发布及文件大小：[量化发布者模型卡](https://huggingface.co/bartowski/NousResearch_Hermes-4-14B-GGUF/blob/main/README.md)。这是具体量化发行版，不是所有量化格式的统一内存承诺。
- **S3** Qlib 运行方式：[官方 Quick Start](https://github.com/microsoft/qlib/blob/main/docs/introduction/quick.rst)。
- **S4** GPL 私人修改与公开源代码问题：[GNU FAQ](https://www.gnu.org/licenses/gpl-faq.html#GPLRequireSourcePostedPublic)。本次取得搜索索引摘要，直接打开页面超时；采用前应核对完整条款。
- **S5** AGPL 使用与网络交互：[GNU 许可使用说明](https://www.gnu.org/licenses/gpl-howto.en.html)、[AGPLv3 原文](https://www.gnu.org/licenses/agpl-3.0.html)。原文直接打开超时；本次未形成具体项目的法律结论。
- **S6** Backtrader 项目及 GPL-3.0 标识：[上游仓库](https://github.com/mementum/backtrader)。
- **S7** DuckDB 嵌入式并发边界：[LTS 并发文档](https://duckdb.org/docs/lts/connect/concurrency)。新版本的远程协议能力不作为本方案增加服务器组件的理由。
- **S8** Tushare 年费、积分及独立权限：[官方频次权限表](https://tushare.pro/document/1?doc_id=290)、[接口权限说明](https://tushare.pro/document/1?doc_id=108)。

## 10. 审计收尾状态（v2）

- 已完成：基线定位、重点设计检查、部分上游事实核对、问题分级；v2 按用户要求修正成本目标及模型分工，并增加公开数据包接入审计。
- 未完成：主设计改写、依赖全量审查、API 实测、代码实现、回测实验、设备性能测试和恢复演练。
- 建议下一步：GPT 制定 G0 任务，由 DeepSeek/MiMo 执行公开数据包验收及数据/交易契约修订，再建设复盘与研究闭环。
- 提交状态：上一版 GitHub 写入返回 403（Resource not accessible by integration）；当前修订尚未提交，等待连接的仓库写入权限可用。


## 11. investment_data 公开数据包接入审计

### 11.1 当前核查结果

用户提供：[chenditc/investment_data Releases](https://github.com/chenditc/investment_data/releases)。

本次核查仓库代码 commit 为 `b8c129b4d9b838f050eac8b1135111b3b79fc894`。核查 README、Release API、行情表说明、导出代码、标准化代码和归档校验器；**未下载约 566MB 的完整数据包，未运行数据质量验收**。

| 项目 | 核查结果 | 结论边界 |
|---|---|---|
| 最近发布 | Release API 返回 `2026-09-22`，发布时间 `2026-09-22T08:17:39Z` | 发布日期不是实际最后交易日，须读取 manifest 和数据 |
| 正式资产 | `qlib_bin.tar.gz`（566,183,981 bytes）与 `qlib_bin.manifest.json`（511 bytes） | 是已有 Qlib 数据包，可优先评估为历史初始化底座；不是已证实的逐日增量包 |
| 归档 API digest | `sha256:7a5e563f8f3bd696f8455e42a1eb36b07ac1272a7d0c43db5f0c099755c820a3` | 来源于 API 元数据，本次未对下载字节独立重算 |
| 校验能力 | 校验器要求归档哈希、长度、来源 commit、目标交易日；检查交易日历和股票池文件 | 能检查发布完整性，不能证明每只股票每个字段正确 |
| 历史起点 | 当前标准化代码设置交易日历起点 `2000-01-04` | 不能据此声称从 1990 年以来全部行情；各证券实际起止日期待包内核验 |
| 价格处理 | 导出后进入 Qlib 标准化，包含价格、成交量、VWAP 等调整逻辑 | 不应把 Qlib 数值直接当作券商报价、股数或金额 |
| 来源 | 项目说明初始导入涉及 Wind、财汇、Tushare、Yahoo；每日更新说明使用 Tushare | 不是官方交易所原始数据库，来源和历史口径要记录 |
| 使用方式 | README 提供直接下载发布资产；自行跑更新脚本需要 Tushare token | 消费公开发布物与自行复制上游生产流水线分开设计 |
| 许可 | 仓库显示 Apache-2.0 代码许可 | 不据此推定所有上游数据都获得同等再分发授权；具体数据条款待核实 |

### 11.2 推荐接入路径

1. 固定明确的 release tag，下载同一 Release 的 archive 与 manifest；记录 URL、asset ID、发布时间、下载时间、大小及 SHA-256。
2. 使用审阅过且固定 commit 的 `qlib/validate_archive.py`，按 README 的 `--expected-tag` 与 `--require-publishable` 方式校验。脚本本身及依赖也纳入版本记录。
3. 解压到新的独立快照目录；不直接覆盖现有工作数据。核查成员路径、日历、股票池及目标日期，完成第 11.3 节后原子切换活动快照。
4. Qlib 直接读取该快照；Parquet/DuckDB 只保存补充数据、审计索引与需要统一查询的视图，避免重复格式转换。
5. 每日先检查是否有新发布。相同 digest 不重复下载；有新包则验证后切换。暂按全包快照处理，不假定只追加一天就能吸收上游历史修订。
6. Release 延迟或失败时继续保留旧快照并标明截至日期。BaoStock/AKShare 补充数据放独立层，明确优先级、单位和复权口径，不静默拼接进第三方二进制包。
7. 记录 manifest 中数据和代码来源标识；固定实验使用的实际快照，即使未来同 tag 内容变化也不影响已完成实验的可追溯性。

### 11.3 放行前必须执行的检查

| 检查 | 方法 | 放行条件 |
|---|---|---|
| 起止日期与覆盖 | 统计交易日历、每只证券首末有效值、交易所/板块/上市退市状态及空值比例 | 形成实际覆盖报告，研究区间不超出已验收范围 |
| 最新日期 | 对照 manifest 的 target_trade_date、day.txt 和股票有效行情最大日期 | 日历末日不能掩盖大量个股陈旧行情 |
| 历史与退市股 | 选取已知退市、暂停上市、长停牌和新股案例，核对历史范围 | 不仅仅是“all.txt 存在”；缺失显式登记，不能声称无幸存者偏差 |
| 复权及单位 | 对照 normalize.py 和所固定 Qlib 实现，抽样除权前后价格、量、额及 VWAP | 研究价格与交易价格可区分，量价单位可解释，不重复复权 |
| 多源口径 | 抽样对照独立公开源，重点看历史拼接、除权和异常日期 | 差异解释或隔离，不能按一个固定百分比忽略所有差异 |
| 指数成分历史 | 检查 instruments 区间及上游权重转成分逻辑，核对历史样例 | 历史股票池不是当前成分的回填 |
| 缺失与停牌 | 验证缺失值、零成交与停牌标记的区别 | 缺失不默认为零收益或可成交 |
| 财务 PIT 与事件 | 检查是否实际包含原始版本、公告时间和事件证据 | 没有就列为缺口，不以行情包替代 PIT 财务与公告库 |
| 修订与重放 | 比较两次快照的历史交集，对变动生成差异报告 | 历史修订可追踪；旧实验使用旧快照可复现 |

公开日更快照能减少采集成本，也能积累“此快照发布时可见的数据”证据；但不能还原项目归档开始前每一天的财务原始披露版本。

### 11.4 对原方案的具体调整

- 将“从零采集全市场多年日线”改为“公开数据包初始化、定期同步、质量验收与按需补缺”。
- 全市场研究无需仅为省钱而缩到个人持仓，但报告与事件处理可以优先关注持仓。
- 数据生产者所需服务器和 Tushare token 不等于下载使用者也必须配备；首期不部署该项目整套 Dolt/生产发布流水线。
- 股票真实成交价格、交易限制、财务 PIT、公告及行业历史单独建设覆盖矩阵，付费只解决确认存在的缺口。
- 保留原审计的防前视、执行契约、样本外封存和备份要求。这些要求由研究正确性决定，与数据是否免费无关。

### 11.5 新增证据链接

- [S9：固定版本 README](https://github.com/chenditc/investment_data/blob/b8c129b4d9b838f050eac8b1135111b3b79fc894/README.md)
- [S10：2026-09-22 Release](https://github.com/chenditc/investment_data/releases/tag/2026-09-22)
- [S11：标准化与日历起点](https://github.com/chenditc/investment_data/blob/b8c129b4d9b838f050eac8b1135111b3b79fc894/qlib/normalize.py)
- [S12：归档校验器](https://github.com/chenditc/investment_data/blob/b8c129b4d9b838f050eac8b1135111b3b79fc894/qlib/validate_archive.py)
- [S13：日线表来源与拼接说明](https://github.com/chenditc/investment_data/blob/b8c129b4d9b838f050eac8b1135111b3b79fc894/docs/final_a_stock_eod_price.ch.md)
- [S14：行情导出查询](https://github.com/chenditc/investment_data/blob/b8c129b4d9b838f050eac8b1135111b3b79fc894/qlib/dump_all_to_qlib_source.py)
